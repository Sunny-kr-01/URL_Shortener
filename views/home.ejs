<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body style="background-color: #121212;">
    <h1 style="color:#fff">URL Shortener</h1>
    <div>
        
        <form action="/url" method="post">
            <label for="" style="color: #fff;">Enetr you original url : </label>
            <input type="text" 
            name="url" 
            placeholder="https//example.com"
            />
            <button
            type="submit">Generate</button>
        </form>
    </div>
    <% if (locals.ID) { %>
         <p style="color: #fff;">URL generated : http://localhost:67/url/<%= ID %></p>   
    <% } %>
    <div style="margin-top: 30px">
      <% if (locals.urls) { %>
      <table>
        <thead>
          <th style="color: #fff;">S. No</th>
          <th style="color: #fff;">ShortID</th>
          <th style="color: #fff;">Redirect</th>
          <th style="color: #fff;">Clicks</th>
        </thead>
        <tbody>
          <% urls.forEach((url, index) => { %>
          <tr>
            <td style="color: #fff;"><%= index + 1 %></td>
            <td style="color: #fff;"><%= url.shortID %></td>
            <td style="color: #fff;"><%= url.redirectURL %></td>
            <td style="color: #fff;"><%= url.clickHistory.length %></td>
          </tr>
          <% }) %>
        </tbody>
      </table>
      <% } %>
    </div>
</body>
</html>